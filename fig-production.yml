redis:
  image: redis
  ports:
    - "6379"
web:
  build: /home/albums/current
  command: bundle exec foreman start -f Procfile.production
  volumes:
    - /homes/album/current:/aoty
  ports:
    - "3000:3000"
  links:
    - redis
  environment:
    RAILS_ENV: production
